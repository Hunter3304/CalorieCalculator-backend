# PostgreSQL ??? URL?????? 5432
# URL für die PostgreSQL-Datenbankverbindung, Standardport ist 5432
spring.datasource.url=jdbc:postgresql://localhost:5432/calorie_calculator

# ?? PostgreSQL ????????? postgres
# Dein PostgreSQL-Benutzername, normalerweise standardmäßig postgres
spring.datasource.username=postgres

# ?? PostgreSQL ?? (???????????)
# Dein PostgreSQL-Passwort (bitte durch dein lokales, echtes Passwort ersetzen)
spring.datasource.password=123456

# ?? PostgreSQL ????
# Spezifiziere den PostgreSQL-Treiber
spring.datasource.driver-class-name=org.postgresql.Driver

# ?? MyBatis ???????? (?????????)
# Aktiviere das automatische CamelCase-Mapping für MyBatis (weiterhin beibehalten, sehr nützlich)
mybatis.configuration.map-underscore-to-camel-case=true
